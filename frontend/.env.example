# Backend API base URL — required in production (Vercel), leave empty for local dev (uses Vite proxy)
VITE_API_URL=https://your-backend.onrender.com

# Set to true only for local dev where mysqldump exists — backup UI is hidden when unset or false
VITE_ENABLE_BACKUPS=false
